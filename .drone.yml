pipeline:
  ssh:
    image: appleboy/drone-ssh
    settings:
      host: drone-deploy.webfsd.com
      username: deployer
      key:
        from_secret: ${SSH_KEY}
      port: 32
      script:
        - echo "hello"
        - echo "world"
